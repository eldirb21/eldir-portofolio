const initialize = {
  loading: false,
};
export const projectReducer = (state = initialize, action) => {
  switch (action.type) {
    case "1":
      return state;
    default:
      return state;
  }
};
